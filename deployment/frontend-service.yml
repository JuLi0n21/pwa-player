apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: osu-music
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 5001
  type: NodePort
